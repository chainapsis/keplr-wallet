import styled from "styled-components";
import { Box } from "../box";

export const Styles = {
  Title: styled.div`
    cursor: pointer;

    margin-bottom: 0.5rem;
  `,
  Items: styled(Box)`
    margin-top: 0.5rem;
  `,
};
