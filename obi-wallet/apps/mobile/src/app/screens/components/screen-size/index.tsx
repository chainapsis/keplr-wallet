import { Dimensions } from "react-native";

export const { width: screenWidth, height: screenHeight } =
  Dimensions.get("window");

export const smallDeviceMaxWidth = 375;
export const smallDeviceMaxHeight = 667;
