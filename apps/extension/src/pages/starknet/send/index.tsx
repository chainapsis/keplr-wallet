import React, { FunctionComponent } from "react";
import { observer } from "mobx-react-lite";

export const StarknetSendPage: FunctionComponent = observer(() => {
  return <div>StarknetSendPage</div>;
});
